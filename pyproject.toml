[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fuzzyschema"
version = "0.1.0"
dependencies = ["ex-fuzzy", "numpy", "pymoo"]

[tool.setuptools]
packages = ["fuzzyschema"]
