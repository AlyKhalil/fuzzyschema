[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fuzzyschema"
version = "0.1.0"
dependencies = ["ex-fuzzy", "numpy", "pandas", "pymoo>=0.6.0", "joblib"]

[tool.setuptools]
packages = ["fuzzyschema"]
